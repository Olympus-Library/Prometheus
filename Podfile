source 'https://github.com/CocoaPods/Specs.git'

workspace 'Prometheus'
xcodeproj 'Prometheus'

target :'Prometheus iOS' do
  platform :ios, "7.0"
  link_with 'Prometheus iOS'
  podspec
end

target :'Prometheus OSX' do
  platform :osx, "10.9"
  link_with 'Prometheus OSX'
  podspec
end

